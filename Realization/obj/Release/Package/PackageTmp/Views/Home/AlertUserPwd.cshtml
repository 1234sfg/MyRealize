
@{
    ViewBag.Title = "修改密码";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="xyz_alertmima">
    <form action="/Home/AlertUserPwd" method="post" onsubmit="return submit_sure()">

        <div class="xyz_line">
            <div class="xyz_line_left">
                新密码：
            </div>
            <div class="xyz_line_right">
                <input type="password" name="NewPwd" placeholder="请输入新密码" required />
            </div>
        </div>
        <div class="xyz_line">
            <div class="xyz_line_left">
                确认密码：
            </div>
            <div class="xyz_line_right">
                <input type="password" name="ConfirmPwd" placeholder="请再输入一次密码" required />
            </div>
        </div>
            <input type="submit" class="xyz_input_submit" value="确认修改" name="AlertPwd" />
    </form>
</div>
<script>
    function HomeB() { }
    function submit_sure() {
        var cf = confirm("确定要进行这个操作吗?");
        if (cf == true) {
            return true;
        } else {
            return false;
        }
    }
</script>

<style scoped>
    .xyz_alertmima {
        
        min-height: 400px;
    }
        .xyz_alertmima form {
            display: flex;
            flex-direction: column;
            justify-content:center;
            align-items:center;
        }
    .xyz_input_submit {
        width: 100px;
        height: 30px; 
        margin-top: 20px;
        text-decoration: none;
        font: bold 16px 'Trebuchet MS',Arial, Helvetica;
        display: inline-block;
        text-align: center;
        color: #fff;
        border: 1px solid rgba(0, 0, 0, 0.3);
        text-shadow: 0 1px 0 rgba(0,0,0,0.4);
        box-shadow: 0 0 0.05em rgba(0,0,0,0.4);
        -moz-box-shadow: 0 0 .05em rgba(0,0,0,0.4);
        -webkit-box-shadow: 0 0 0.05em rgba(0,0,0,0.4);
        border-radius: .3em;
        background: -webkit-gradient(linear, left top, left bottom, from(#656565), to(#141414) );
    }
    .xyz_line_left {
        width: 85px;
        font-family: "宋体";
        display: flex;
        align-items: center;
        font-size: 14px;
        text-align: justify;
        font-weight: bold;
    }

    .xyz_line_right {
    }

        .xyz_line_right input {
            outline: none;
        }

    .xyz_line {
        display: flex;
        margin-top:20px;
    }
</style>